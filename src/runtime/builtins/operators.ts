
// import { sumFunction } from "./builtins.js";

// export const __load_operators = () => {

// sumFunction('infix||', ['logical-or']);
// sumFunction('infix&&', ['logical-and']);

// sumFunction('infix!=', ['not-equal']);
// sumFunction('infix==', ['equals']);
// sumFunction('infix=~', ['almost-kinda-equals']);

// sumFunction('infix<', ['less-than']);
// sumFunction('infix<=', ['less-than-or-equal']);
// sumFunction('infix>', ['greater-than']);
// sumFunction('infix>=', ['greater-than-or-equal']);

// sumFunction('infix>>', ['bitshift-right']);

// sumFunction('infix+', ['add', 'concatenate']);
// sumFunction('infix-', ['sub']);
// sumFunction('infix*', ['mult', 'repeat', 'church-iterate']);
// sumFunction('infix/', ['div']);
// sumFunction('infix%', ['mod']);

// sumFunction('infix**', ['pow']);
// sumFunction('infix√', ['root']);
// sumFunction('prefix√', ['sqrt']);
// sumFunction('prefix∛', ['cbrt']);
// sumFunction('prefix∜', ['qdrt']);

// sumFunction('infix->', ['map']);

// sumFunction('infix.', ['compose']);
// sumFunction('infix|', ['coalesce']);

// sumFunction('prefix!', ['not']);
// sumFunction('prefix-', ['negate']);
// sumFunction('prefix#', ['range', 'length']);

// sumFunction('postfix!', ['fact']);

// }